name: Ping Streamlit App

on:
  schedule:
    - cron: "0 */3 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Acordar o Dashboard
        run: curl -sL https://monitor-b3.streamlit.app/_stcore/health
