name: Ping Streamlit App

on:
  schedule:
    - cron: "0 */3 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Bater na porta para acordar o app
        run: curl -s https://monitor-b3.streamlit.app/ > /dev/null || true
